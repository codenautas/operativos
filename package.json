{
  "name": "operativos",
  "description": "Elementos base para operativos estadísticos",
  "version": "0.5.5",
  "author": "Codenautas <codenautas@googlegroups.com>",
  "repository": "codenautas/operativos",
  "license": "MIT",
  "main": "dist/server/app-operativos.js",
  "types": "dist/server/app-operativos.d.ts",
  "files": [
    "dist",
    "install",
    "unlogged"
  ],
  "dependencies": {
    "backend-plus": "~0.29.24",
    "pg-triggers": "~0.2.3",
    "best-globals": "~0.10.15"
  },
  "devDependencies": {
    "types.d.ts": "~0.4.1"
  },
  "engines": {
    "node": ">= 10.0.0"
  },
  "scripts": {
    "prepare": "npm run build",
    "build-without-err": "(tsc -p tsconfig-client.json || echo \"continue w/error\") && (tsc -p tsconfig-server.json  || echo \"continue w/error\")",
    "build": "tsc -p tsconfig-client.json && tsc -p tsconfig-server.json",
    "start": "node dist/server/server-operativos.js"
  },
  "qa-control": {
    "package-version": "0.3.0",
    "coverage": 90,
    "run-in": "server",
    "stability": "extending",
    "type": "app",
    "ecmaVersion": 6
  }
}
